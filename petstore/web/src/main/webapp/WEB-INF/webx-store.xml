<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:p="http://www.springframework.org/schema/p"
             xmlns:services="http://www.alibaba.com/schema/services"
             xmlns:ml-adapters="http://www.alibaba.com/schema/services/module-loader/adapters"
             xmlns:ml-factories="http://www.alibaba.com/schema/services/module-loader/factories"
             xsi:schemaLocation="
                 http://www.alibaba.com/schema/services http://localhost:8080/schema/services.xsd
                 http://www.alibaba.com/schema/services/module-loader/adapters http://localhost:8080/schema/services-module-loader-adapters.xsd
                 http://www.alibaba.com/schema/services/module-loader/factories http://localhost:8080/schema/services-module-loader-factories.xsd
                 http://www.springframework.org/schema/beans http://localhost:8080/schema/www.springframework.org/schema/beans/spring-beans.xsd
             ">

    <!-- 支持${xxx}替换。 -->
    <services:property-placeholder>
        <property key="component">store</property>
    </services:property-placeholder>

    <!-- 共享配置。 -->
    <beans:import resource="common/webx-component-and-root.xml" />
    <beans:import resource="common/webx-component.xml" />

    <!-- 执行管道。 -->
    <beans:import resource="common/pipeline.xml" />

    <!-- 表单验证。 -->
    <beans:import resource="store/form.xml" />

    <!-- 装载模块。 -->
    <services:module-loader>
        <ml-factories:class-modules>
            <search-packages type="$1" packages="com.alibaba.sample.petstore.web.store.module.*" />
        </ml-factories:class-modules>
    </services:module-loader>

    <!--
      - 页面授权：
      - 默认允许所有screens，包括图片、css等，但不能执行除了CartAction以外的actions。
      - 默认禁止/edit下的screens。
      - 对管理员开放edit/*下的screens、actions。
     -->
    <services:page-authorization default="allow">
        <match target="/">
            <grant user="anonymous, *">
                <deny>action.*</deny>
            </grant>
            <grant user="anonymous, *">
                <allow>action.CartAction.*</allow>
            </grant>
        </match>
        <match target="/edit">
            <grant user="anonymous, *">
                <deny>*</deny>
            </grant>
            <grant role="admin">
                <allow>*</allow>
            </grant>
        </match>
    </services:page-authorization>

</beans:beans>